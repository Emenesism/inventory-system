FROM golang:1.25.7-bookworm

RUN apt-get update \
  && apt-get install -y --no-install-recommends sqlite3 ca-certificates \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /work/backend

ENTRYPOINT ["/usr/local/go/bin/go", "run", "./cmd/import_legacy"]
